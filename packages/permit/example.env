# Permit.io Configuration Environment Variables

# Required: Permit.io authentication
PERMIT_API_KEY=your_permit_api_key

# Optional: Policy Decision Point (PDP) configuration
# Defaults to cloud PDP (https://cloudpdp.api.permit.io)
PERMIT_PDP_URL=http://localhost:7766

# Optional: Project and environment details
# Only needed if you want to specify them explicitly
PERMIT_PROJECT_ID=your_project_id
PERMIT_ENV=your_environment 